<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-700 text-slate-100 flex items-center justify-center">
    <main class="w-full max-w-3xl p-6">
    <div class="grid-cols-2 items-center justify-center p-10 w-full min-h-screen"> 
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class="text-3xl text-black font-semibold"><span class="font-light">Hello</span>, <%= user.name %> 👋🏻</h3>
        </div>
        <h5 class="mb-5 mt-3 font-semibold">you can create new a post.</h5>
        <form method="post" action="/post">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-black font-semibold">
            <label class="block">
                <textarea name="content" placeholder="What's on your mind ?" class="block p-3 outline-none w-1/3 resize-none bg-transparent border-1 border-zinc-700 rounded-md text-black"></textarea>
            </label>
            </div>
            <div class="pt-5">
                <button type="submit" class="w-50% flex items-center justify-center gap-2 rounded-md bg-purple-600 hover:bg-indigo-600 px-4 py-2 text-white font-semibold shadow">Create new post</button>
            </div>
        </form>
    <div class="posts mt-20">
        <h3  class="text-zinc-900">Your Post.</h3>
        <div class="postcontainer mt-5">
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-4 mb-4 rounded-lg bg-slate-700 border border-slate-700">
                    <h4 class="text-green-500 mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-blue-400" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-yellow-500" href="/edit/<%= post._id %>">edit</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    </main>
</body>
</html>