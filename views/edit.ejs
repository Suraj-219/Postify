<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-700 text-slate-100 flex items-center justify-center">
    <main class="w-full max-w-3xl p-6">
    <div class="grid-cols-2 items-center justify-center">
        
        <h5 class="text-2xl mb-5 mt-3 font-semibold text-black">Edit your post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-zinc-700 font-semibold">
                <label class="block">
                    <textarea name="content" placeholder="What's on your mind ?" class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
                </label>
            </div>
            <div class="pt-5">
                <button class="px-3 w-40 py-2 text-sm bg-yellow-500 hover:bg-yellow-600 text-black block rounded-md mt-2" type="submit">Update post</button>
            </div>
        </form>
    </div>
    </main>
</body>
</html>